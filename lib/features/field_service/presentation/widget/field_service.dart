import 'package:flutter/material.dart';

class FieldService extends StatelessWidget {
  const FieldService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("test field service");
  }
}
